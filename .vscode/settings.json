{
    // Enable formatting for C#
    "csharp.format.enable": true,

    // Use the official C# formatter
    "editor.defaultFormatter": "ms-dotnettools.csharp",

    // Format on save (optional but convenient)
    "editor.formatOnSave": true
}
